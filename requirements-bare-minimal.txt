# Bare minimal requirements (no Rust, no complex dependencies)
fastapi==0.104.1
uvicorn==0.24.0
starlette==0.27.0

# HTTP and networking (pure Python)
requests==2.31.0

# Environment and configuration
python-dotenv==1.0.0

# Utilities
python-multipart==0.0.6 